# Local development (APP_ENV=dev)
# Do not commit real secrets — use placeholders; override locally if needed (untracked copy or env).
http_port: "8080"
gin_mode: debug

database_url: "postgres://postgres:sai123@localhost:5432/carmanage?sslmode=disable"

jwt_secret: "change-me-in-development-only"
jwt_ttl_hours: 72

allow_self_kyc_verify: true

azure:
  storage_account: ""
  storage_key: ""
  storage_container: ""

twilio:
  account_sid: ""
  auth_token: ""
  from_number: ""
