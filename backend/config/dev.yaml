# Development / hosted (APP_ENV=dev) — backend/.env must only set APP_ENV=dev.
# Neon Postgres (cloud). For local Postgres instead, swap database_url (see comment below).

http_port: "8080"
gin_mode: debug

database_url: "postgresql://neondb_owner:npg_QE7YkAOJ4XqL@ep-delicate-block-aqphxz9v.c-8.us-east-1.aws.neon.tech/neondb?sslmode=require"
# Local Postgres alternative:
# database_url: "postgres://postgres:postgres@localhost:5432/carmanage?sslmode=disable"

jwt_secret: "carmanage-stag-jwt-8f3a2b1e9d4c7a6f5e0b2d8c1a4f7e9"
jwt_ttl_hours: 72

allow_self_kyc_verify: true

azure:
  storage_account: ""
  storage_key: ""
  storage_container: ""

twilio:
  account_sid: ""
  auth_token: ""
  from_number: ""

payments:
  customer_commission_percent: 2
  owner_commission_percent: 1.5
  gst_percent_on_commission: 18

razorpay:
  key_id: "rzp_test_SqOnnK1iP8U6Qb"
  key_secret: "NIyBNNuGDvlkv1TIfpCaQBmN"

app:
  public_url: http://localhost:3000
  deployed_public_url: https://cm-p2-p-frontend.vercel.app

cors:
  allowed_origins:
    - http://localhost:3000
    - http://127.0.0.1:3000
    - https://cm-p2-p-frontend.vercel.app
